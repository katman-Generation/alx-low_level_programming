#include <stdio.h>
/**
 * main - prints Use the function printf
 * Your program should return 0
 * Return: 0 if exited propery , non-zero otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
